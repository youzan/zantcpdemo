#!/usr/bin/env php
<?php


//$r = nova_encode("com.youzan.service.test", 'pong', 0, 0, 0, "", "", $data);
//var_dump($r);
//var_dump($data);
//exit;

$app = require_once __DIR__.'/../init/app.php';

$server = $app->createTcpServer();
$server->start();

